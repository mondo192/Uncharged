using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CameraCutScene : MonoBehaviour {

	// Use this for initialization
	void Start () {
		//Get player component and disable player controller until camera movement is over. Also can have different colors and the like for the pan over
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
